<h1>My Tasks</h1>



<% @tasks.each do |task| %>
<div>
  <input type="checkbox" id="<%= task.id %>" <%= 'checked' if task.completed == true %> >
  <label for="<%= task.id %>"><%= link_to task.title, task_path(task)%></label>
</div>
  <% end %>

<div>
  <%= link_to new_path do  %>
    <i class="fas fa-plus-square mr-2"></i>Add a new task
  <% end %>
</div>


